# Google Apps Script web app URL for order intake (sheet + email).
# REQUIRED for customers to receive an order confirmation email.
# While this is empty the form falls back to FormSubmit.co, which notifies the
# bakery but does NOT reliably deliver the customer confirmation.
# See README → "Order intake (email + Google Sheet)".
VITE_ORDER_INTAKE_URL=
